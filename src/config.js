export const GMAIL_CONFIG = {
  SEARCH_QUERY_FROM: 'noreply@youtube.com',
  SEARCH_QUERY_SUBJECT_PREFIX: 'subject:',
};

export const SPREADSHEET_CONFIG = {
  // In the future, we can specify a sheet name, e.g., SHEET_NAME: 'Subscribers'
  // and column headers, e.g., HEADERS: ['Date', 'Subscriber', 'Channel']
};
